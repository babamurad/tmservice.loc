# Backend (Laravel 13, Sanctum) — единый план

Статус: `[x]` сделано и проверено по коду · `[~]` сделано частично / небезопасно · `[ ]` не начато.

---

## П0. Закрыть уязвимость в регистрации (СЕЙЧАС, до любых других шагов)

**Факт**: `app/Http/Controllers/AuthController.php@register` валидирует
`role` как `'required|string|in:admin,master,client'` и создаёт пользователя
с этой ролью напрямую из тела запроса. Любой может отправить
`{"role": "admin"}` и получить полный доступ к будущей админке. Это не
гипотетический риск — код уже так работает сейчас.

- [x] П0.1: Убрать `role` из полей, принимаемых при регистрации напрямую в контроллере.
- [x] П0.2: Разрешить публичной регистрации только `client`/`master`.
  Сделано вариантом 2: `'role' => 'required|string|in:client,master'` в
  `RegisterRequest` — `admin` в списке не участвует ни при каком теле запроса.
- [x] П0.3: Роль `admin` — только через `php artisan make:admin {phone}`
  (создаёт нового пользователя или повышает существующего, с подтверждением)
  или сидер. Никогда через HTTP.
- [x] П0.4: Валидация вынесена в `RegisterRequest` (`FormRequest`) —
  `app/Http/Requests/RegisterRequest.php`, `AuthController@register`
  использует `$request->validated()`.

Покрыто тестами: `tests/Feature/RegisterRoleTest.php` — `role: admin` → `422`,
`client`/`master` регистрируются нормально, `make:admin` создаёт админа.
Полный набор тестов (`php artisan test`) — зелёный.

**Критерии приёмки**: запрос с `"role": "admin"` → `422`, либо роль
принудительно `client`/`master`. Создать `admin` через публичный API
невозможно ни при каком теле запроса.

---

## Этап 1. База данных и модели

- [x] Шаг 1.1: `User` — `id`, `phone` (unique), `password`, `role` (enum), `remember_token`, timestamps.
- [~] Шаг 1.2: `City` — `id`, `name_ru`, `name_tm`, `is_active`. Сидер: Туркменабад, Ашхабад, Мары, Дашогуз, Балканабад — **сделано**.
  - [ ] Шаг 1.2.1 (новое, см. `README.md` п.1): миграция — добавить
    `parent_city_id` (nullable, `foreignId(...)->nullable()->constrained('cities')->nullOnDelete()`).
  - [ ] Шаг 1.2.2: пополнить `CitySeeder` посёлками-спутниками Туркменабада
    (уточнить у заказчика точный список — Фарап, Сеятлы, Билас и т.д.),
    с `parent_city_id` на запись Туркменабада.
- [x] Шаг 1.3: `Category` — `id`, `name_ru`, `name_tm`, `icon_url`, `is_active`. Сидер: Сантехник, Электрик, Ремонт авто, Сборка мебели.
- [x] Шаг 1.4: `MasterProfile` — `belongsTo(User)`, `city_id`, `category_id`, `bio`, `is_free`, `qr_code_path`.
- [x] Шаг 1.5: `PortfolioImage` — `belongsTo(MasterProfile)`, `image_path`.

---

## Этап 2. Авторизация (Sanctum)

- [x] Шаг 2.1: Sanctum установлен и настроен.
- [~] Шаг 2.2: `AuthController@register` — работает, но **см. П0** (роль из клиента — небезопасно).
- [x] Шаг 2.3: `AuthController@login`.
- [x] Шаг 2.4: `AuthController@logout`.

### Этап 2A. Верификация телефона (OTP) — до публичного запуска

> **Открытый вопрос — SMS-шлюз для Туркменистана.** Доставка SMS на `+993`
> через международные шлюзы (Twilio и т.п.) может быть нестабильной —
> телеком в стране ограничен. Прежде чем писать OTP-логику — отдельный
> спайк: проверить доставку через 1–2 шлюза-кандидата на живой `+993`-номер.
> По подтверждению пользователя — намеренно отложено до момента, когда
> дойдёт очередь именно до этого этапа; остальной план от решения не зависит.

- [ ] Шаг 2A.1: Таблица `phone_verifications` — `id`, `phone` (index), `code` (4–6 цифр), `expires_at`, `attempts` (default 0), timestamps.
- [ ] Шаг 2A.2: `POST /api/auth/send-otp` — генерирует код (TTL 5 мин), пишет в таблицу, отправляет SMS.
- [ ] Шаг 2A.3: `POST /api/auth/verify-otp` — сверяет код/срок, лимит попыток (например 5).
- [ ] Шаг 2A.4: `users.phone_verified_at` (nullable timestamp). Пользователь виден в каталоге только после верификации.
- [ ] Шаг 2A.5: Интерфейс `SmsGateway` с реализациями:
  - `LogSmsGateway` — local/staging, код в лог, реальные SMS не уходят.
  - Реальный провайдер — по итогам спайка.
  - Запасной план на случай нерабочей доставки на `+993`: ручная модерация
    мастера админом (см. Этап C2.3) или подтверждение звонком/мессенджером.
- [ ] Шаг 2A.6: Антиспам на `send-otp` — не чаще 1/60 сек на номер (см. Этап 2B — rate limiting).

**Критерии приёмки**: без подтверждённого кода — не попасть в выдачу
мастеров. На local — код виден в логах, реальные SMS не уходят. Подбор
кода ограничен по попыткам и времени жизни.

### Этап 2B. Rate limiting — до публичного запуска

**Проблема**: `GET /api/masters/{id}` отдаёт телефон публично (by design),
но без лимита базу легко спарсить. `login`/`send-otp` без лимитов открыты
для брутфорса и SMS-флуда (SMS — это ещё и деньги).

- [x] Шаг 2B.1: Именованные лимитеры — сделано в `app/Providers/AppServiceProvider.php@boot()` (не в `bootstrap/app.php` — там для этого нет подходящего хука в Laravel 13, `RateLimiter::for` регистрируется в провайдере):
  ```php
  RateLimiter::for('public-read', fn (Request $r) => Limit::perMinute(60)->by($r->ip()));
  RateLimiter::for('auth',        fn (Request $r) => Limit::perMinute(5)->by($r->ip()));
  RateLimiter::for('otp',         fn (Request $r) => Limit::perMinutes(60, 5)->by($r->input('phone')));
  ```
  `otp`-лимитер зарегистрирован заранее, но пока ни на один маршрут не навешан — `send-otp`/`verify-otp` появятся в Этапе 2A, тогда же и подключить `throttle:otp`.
- [x] Шаг 2B.2: `throttle:public-read` на `GET /api/masters`, `GET /api/masters/{id}` (`routes/api.php`).
- [x] Шаг 2B.3 (частично): `throttle:auth` на `login`/`register`. `throttle:otp` — отложено до Этапа 2A (маршрутов ещё нет).

Покрыто тестами: `tests/Feature/RateLimitTest.php` — 6-я подряд неудачная
попытка логина за минуту → `429` с `Retry-After`; 61-й запрос `GET /api/masters`
за минуту → `429`. Полный набор тестов (`php artisan test`) — зелёный.

**Критерии приёмки**: превышение лимита → `429` с `Retry-After`. Массовый перебор `masters/{id}` упирается в лимит.

---

## Этап 3. API-эндпоинты

- [x] Шаг 3.1: `DirectoryController` — `GET /api/cities`, `GET /api/categories`, кэш `Cache::remember` (24ч) — сделано.
  - [ ] Шаг 3.1.1: Инвалидация кэша при изменении справочников из админки (см. Этап C2).
  - Уточнение: этот кэш — оптимизация нагрузки на БД, а не «офлайн» — офлайн реализуется на клиенте (`02-mobile.md`).
- [~] Шаг 3.2: `MasterController@index`/`show` — базовая версия с фильтрами `city_id`/`category_id`, сортировкой по `is_free`, пагинацией — сделано. Осталось:
  - [ ] `city_id` учитывает `parent_city_id` (см. Шаг 1.2.1 / `README.md` п.1) — головной город включает мастеров из посёлков-спутников.
  - [ ] rate limiting (Этап 2B).
  - [ ] фильтр `moderation_status = approved` (Этап C2.3).
  - [ ] поиск `?q=` (Этап C1).
  - [ ] поля `avg_rating`/`reviews_count` (Этап D).
- [x] Шаг 3.3: `ProfileController` (`auth:sanctum`) — `update`, `status` (тумбл. `is_free`), `portfolio` (загрузка + сжатие через Intervention Image → webp) — сделано.
  - [ ] Удаление фото из портфолио и лимит по числу — см. Этап C3.

---

## Этап 4. QR-коды

- [~] Шаг 4.1–4.2: `ProfileController@generateQr` генерирует `.png` через `endroid/qr-code`, сохраняет в `storage/app/public/qr` — сделано технически, **но кодирует `appscheme://master/{id}`** — см. проблему ниже.

### Этап 4A. QR через Universal Links / App Links (замена кастомной схемы)

**Проблема**: `appscheme://master/{id}` срабатывает только если приложение
уже установлено. Клиент сканирует визитку без приложения — ссылка молча
ничего не делает. Для QR, чей смысл — привлекать новых клиентов, это
провал сценария.

- [ ] Шаг 4A.1: QR кодирует `https://{домен}/m/{id}` вместо кастомной схемы.
- [ ] Шаг 4A.2: Веб-страница `/m/{id}` (единственное исключение из
  «только JSON» — можно Blade): если приложение установлено → ОС сама
  откроет его; если нет → карточка мастера + кнопки «Открыть в приложении /
  App Store / Google Play».
- [ ] Шаг 4A.3: Файлы ассоциации доменов:
  - iOS: `https://{домен}/.well-known/apple-app-site-association` (без расширения, `Content-Type: application/json`).
  - Android: `https://{домен}/.well-known/assetlinks.json`.

**Критерии приёмки**: скан QR с приложением → открывается `MasterDetailScreen` нужного мастера. Без приложения → веб-страница с переходом в стор.

---

## Этап 5. Роль `admin` — админка и модерация (MVP)

**Проблема**: роль `admin` заведена в модели, но не имеет ни одного
эндпоинта/интерфейса — сейчас управлять справочниками и мастерами некому.

- [ ] Шаг 5.1: Инструмент — рекомендуется **Filament** (готовая админка на
  Laravel, быстрый CRUD без отдельного фронта). Альтернатива —
  защищённые `admin`-маршруты API, если админку решат делать отдельным фронтом.
- [ ] Шаг 5.2: CRUD городов/категорий (`is_active`) → сброс кэша (Шаг 3.1.1).
- [ ] Шаг 5.3: Список мастеров: блокировка/разблокировка, просмотр жалоб.
- [ ] Шаг 5.4: `master_profiles.moderation_status` (enum `pending`/`approved`/`rejected`, default `pending`). В `GET /api/masters` — только `approved`. Закрывает и запасной сценарий верификации из Этапа 2A.5 (ручная модерация вместо/вместе с OTP).
- [ ] Шаг 5.5: Middleware `admin` — любой не-admin → `403`.

**Критерии приёмки**: админ включает/выключает города, категории,
одобряет/блокирует мастеров. Неодобренный мастер не виден в каталоге.

---

## Этап 6. Поиск (`SearchTab`)

Мобильный таб `Search` уже существует в навигации и есть экран
`SearchScreen.js` — со стороны бэкенда поиска ещё нет.

- [ ] Шаг 6.1: Расширить `MasterController@index`: `?q=` — поиск по `bio` и
  имени мастера (`LIKE` или полнотекстовый), комбинируется с
  `city_id`/`category_id`.

**Критерии приёмки**: `GET /api/masters?q=...` возвращает релевантных мастеров, фильтры комбинируются.

---

## Этап 7. Управление портфолио (удаление/лимит)

- [ ] Шаг 7.1: `DELETE /api/profile/portfolio/{id}` (`auth:sanctum`) — проверка владения (чужое фото → `403`), удаление файла и записи.
- [ ] Шаг 7.2: Лимит фото на профиль (например, 10) — проверка при загрузке.

**Критерии приёмки**: мастер удаляет своё фото, чужое — не может. Превышение лимита — понятная ошибка.

---

## Этап 8. Отзывы и рейтинги (рекомендуется в MVP)

**Проблема**: сейчас клиент выбирает мастера по имени + точке
«свободен/занят» — это про занятость, а не про качество. Без рейтинга нет
сигнала, хорош мастер или нет, а доверие в этой нише — весь продукт.

> **Открытый вопрос — кто может оставлять отзыв.** Подтвердить факт
> оказанной услуги нечем технически (звонок идёт через системную звонилку,
> сделок в приложении нет). Варианты: (а) любой верифицированный клиент,
> один отзыв на пару клиент↔мастер + модерация; (б) отложить до появления
> заявок/заказов в приложении. Для MVP проще (а). Решить до реализации этапа.

- [ ] Шаг 8.1: Модель `Review` — `client_id`, `master_profile_id`, `rating` (1–5), `comment` (nullable), `moderation_status`. Уникальный индекс `(client_id, master_profile_id)`.
- [ ] Шаг 8.2: Денормализация в `master_profiles`: `avg_rating`, `reviews_count` — пересчёт через observer/событие.
- [ ] Шаг 8.3: `POST /api/masters/{id}/reviews` (только верифицированный `client`) → `pending`. `GET /api/masters/{id}/reviews` — одобренные, с пагинацией. Добавить `avg_rating`/`reviews_count` в `GET /api/masters(/{id})`.
- [ ] Шаг 8.4: Сортировка `GET /api/masters`: сначала `is_free = true`, затем по `avg_rating` убыв.
- [ ] Шаг 8.5: Модерация отзывов в админке (Этап 5).

**Критерии приёмки**: клиент — один отзыв на мастера, 1–5 звёзд. Рейтинг и
число отзывов видны в списке/карточке. Неодобренные отзывы не показываются
публично.

---

## Сводная таблица зависимостей

| Что делаем | Зависит от | Блокирует |
|---|---|---|
| П0 (роль при регистрации) | — | Всё остальное — это открытая уязвимость прямо сейчас |
| Этап 2A (OTP) | Спайк SMS-шлюза | Публичный запуск, Этап 5.4 (частично — запасной план без OTP) |
| Этап 2B (rate limiting) | — | Публичный запуск |
| Этап 1.2.1 (parent_city_id) | — | Мобильный UI выбора города (`02-mobile.md`) |
| Этап 4A (QR https) | Домен сервиса, Expo `app.json` (см. `02-mobile.md`) | Полноценный сценарий QR |
| Этап 5 (админка) | — | Этап 5.4 (модерация), инвалидация кэша Шаг 3.1.1 |
| Этап 8 (отзывы) | Этап 2A (нужен verified client) | — |
