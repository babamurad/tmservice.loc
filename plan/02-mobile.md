# Мобильное приложение (Expo / React Native) — единый план

Решение зафиксировано в `README.md`, п.2: остаёмся на Expo/React Native,
не переходим на Vue-стек. Причина коротко — в `ServiceApp/` уже рабочий
стек (Expo 56, RN 0.85, React Navigation 7) и ~10 экранов, переписывать
на Ionic+Vue значит выбросить сделанное ради смены синтаксиса, а не ради
нового результата.

Статус: `[x]` сделано и проверено по коду · `[~]` частично · `[ ]` не начато.

---

## Этап 1. Инициализация и навигация

- [x] Шаг 1.1: `npx create-expo-app ServiceApp` — сделано.
- [x] Шаг 1.2: `@react-navigation/native`, `bottom-tabs`, `native-stack` — установлены.
- [x] Шаг 1.3: Роутинг — `App.js` уже собран точно по плану:
  - `AuthStack` (Login, Register).
  - `MainTabs` (`Catalog` → `CatalogStack`, `Search`, `Profile` → `ProfileStack`).
- [x] Шаг 1.4: `expo-secure-store` + хук `useAuth` (`src/hooks/useAuth.js`) — проверяет токен при старте, переключает `AuthStack`/`MainTabs`.

---

## Этап 2. Экраны клиента (поиск мастеров)

- [x] Шаг 2.1: `CatalogScreen.js` — есть.
- [x] Шаг 2.2: `MastersListScreen.js` — есть.
- [x] Шаг 2.3: `MasterDetailScreen.js` — есть.

Проверить руками (best эффорт — реальным тестированием на устройстве/симуляторе, не только по наличию файла):
- [ ] Кнопка «Позвонить» реально дергает `Linking.openURL('tel:+993XXXXXXXX')`.
- [ ] `MastersListScreen` сортирует «свободен» выше «занят» (сервер уже так отдаёт — см. `01-backend.md` Шаг 3.2).

---

## Этап 3. Экраны мастера (управление визиткой)

- [x] Шаг 3.1: `MyProfileScreen.js`, `EditProfileScreen.js` — есть.
- [x] Шаг 3.2: `expo-image-picker` установлен — загрузка портфолио.
- [x] Шаг 3.3: `MyQRScreen.js` — есть.
  - [x] Проверено после Этапа 4A в `01-backend.md`: `MyQRScreen.js` просто показывает `<Image>` по URL/пути, который вернул сервер (`qr_code_url` или `profile.qr_code_path`) — никакой схемы на клиенте не хардкодит, менять было нечего.
- [x] Кнопка удаления фото — добавлена в `MyProfileScreen` (не в
  `EditProfileScreen` — там нет портфолио, только bio/city_id/category_id).
  Крестик поверх каждой карточки, подтверждение через `Alert.alert`, вызывает
  `DELETE /api/profile/portfolio/{id}` (`01-backend.md` Этап 7) и убирает
  фото из локального состояния без перезагрузки всего профиля.
  По пути поправлен `src/api/client.js`: `api()` падал на `204 No Content`
  (пытался распарсить пустое тело как JSON) — теперь возвращает `null` для
  `204`, иначе кнопка удаления работала бы только на бэкенде, а на клиенте
  всегда показывала «Ошибка».

**Про WebSocket-обновление статуса**: тумблер «Свободен/Занят» — обычный
REST (`POST /api/profile/status`), уже сделано. Laravel Reverb для
мгновенного обновления у всех клиентов — сознательно не в MVP, добавить
отдельным этапом при необходимости (не блокирует текущий план).

---

## Этап 4. Поиск (`SearchScreen`)

- [x] `SearchScreen.js` был заглушкой (текст «Поиск» и ничего больше, подтверждено чтением файла) — переписан на рабочий экран: поле ввода + чипы городов/категорий (переиспользуют `getCachedDirectory` из Этапа 5 — те же данные, что уже лежат в `AsyncStorage`, без лишнего запроса) + список результатов.
- [x] `SearchScreen` дёргает `GET /api/masters?q=&city_id=&category_id=` (`01-backend.md`, Этап 6). Пустой запрос без `q`/фильтров не отправляется — экран не заходит зря на сервер по пустому вводу.
- [x] Навигация: `Search`-таб раньше указывал прямо на `SearchScreen` без стека, поэтому переход в карточку мастера был некуда вести. Обернул в `SearchStack` (`App.js`) с `SearchHome` + `MasterDetail`, как у `Catalog`.

**Критерии приёмки**: таб `Search` открывает рабочий поиск, результат ведёт
на `MasterDetailScreen`. ✅ (не проверено вручную на устройстве/симуляторе —
см. оговорку в Этапе 5, в проекте нет JS-тестовой инфраструктуры).

---

## Этап 5. Offline-first (клиентский персист)

**Важно**: серверный `Cache::remember` в `01-backend.md` — это не офлайн,
это защита БД. Офлайн — это то, что храниться на устройстве.

- [x] Шаг 5.1: `@react-native-async-storage/async-storage` (`^3.1.1`) добавлен в `package.json`. `src/api/directoryCache.js` — хранит `cities`/`categories` в `AsyncStorage` под ключами `directory:data:{cities|categories}` + отдельно последний `ETag` (`directory:etag:{...}`). Без доп. библиотеки состояния — не понадобилась.
- [x] Шаг 5.2: `CatalogScreen` — при монтировании сначала `loadFromCache()` (мгновенно показывает `AsyncStorage`, если там что-то есть, снимает спиннер), параллельно `refreshFromServer()` обновляет с сервера и перезаписывает state/кэш, когда ответ придёт.
- [x] Шаг 5.3: `fetchDirectory()` шлёт `If-None-Match` с сохранённым `ETag` (`01-backend.md`, Шаг 3.1.1/3.1.2). При `304` — отдаёт то, что уже лежит в `AsyncStorage`, не перекачивая тело; при `200` — сохраняет новые данные и новый `ETag`.

**Критерии приёмки**: открытие каталога без сети показывает ранее
загруженные города/категории — ✅ (`loadFromCache()` не зависит от сети).
Повторный запрос без изменений на сервере не тянет полный объём — ✅ (`304`).

Не покрыто автотестами — в `ServiceApp/` пока нет JS-тестовой инфраструктуры
(ни одного `.test.js` в проекте, `jest` не подключён). Логику стоит
проверить руками: открыть `CatalogScreen` на устройстве/симуляторе с сетью
(данные приходят и кэшируются), затем в самолётном режиме (данные остаются
из кэша, спиннер не виснет).

---

## Этап 6. Universal Links / App Links (парный этап к `01-backend.md` Этап 4A)

- [x] Шаг 6.2: `App.js` — `linking`-конфиг на `NavigationContainer`
  (`prefixes: ['serviceapp://']`, `Catalog.MasterDetail: 'm/:id'`).
  `MasterDetailScreen` раньше требовал полный объект `route.params.master`
  (доставал `master.id`) — при переходе по диплинку известен только `id` из
  URL, экран упал бы на `Cannot read properties of undefined`. Теперь читает
  `route.params.id ?? route.params.master.id`, работает и со списком
  (передаёт весь объект), и с диплинком (передаёт только `id`).
  `app.json` получил `"scheme": "serviceapp"` — кастомная схема
  (`serviceapp://m/5`) уже работает в dev/standalone-сборке уже сейчас,
  проверить: `npx uri-scheme open serviceapp://m/5 --ios` (или `--android`)
  после `npx expo prebuild`. Через Expo Go кастомные схемы не тестируются —
  нужен dev-client/standalone build.
- [ ] Шаг 6.1: **заблокировано — нет продакшен-домена.** `tmservice.loc` —
  локальный псевдо-TLD OSPanel, не резолвится извне, не годится для
  `associatedDomains`/`intentFilters`. Бэкенд-часть (`config/deeplink.php`,
  `.well-known/apple-app-site-association`, `/assetlinks.json`, см.
  `01-backend.md` Этап 4A) уже параметризована через `.env` и ничего не
  требует менять в коде. Когда появится домен и подписанная сборка (Apple
  Team ID / Android SHA256 из EAS):
  1. `.env`: `APPLE_APP_BUNDLE_ID`, `APPLE_TEAM_ID`, `ANDROID_PACKAGE_NAME`, `ANDROID_SHA256_CERT_FINGERPRINTS`.
  2. `app.json`: `ios.associatedDomains: ["applinks:{домен}"]`, `android.intentFilters` с `autoVerify: true` на `https://{домен}/m/*`.
  3. В `App.js` `linking.prefixes` добавить `'https://{домен}'` — конфиг `screens` менять не придётся.

**Критерии приёмки**: скан QR с установленным приложением открывает нужный
экран мастера напрямую, без прохождения через веб-страницу. Пока достижимо
только через кастомную схему на собранном dev/standalone-билде — полноценные
Universal Links (открытие по обычной `https`-ссылке без схемы) ждут домена.

---

## Этап 7. Выбор города с учётом посёлков-спутников (парный этап к `01-backend.md` Этап 1.2.1)

- [ ] Шаг 7.1: В `CatalogScreen` — выпадающий список городов показывает
  головные города (Туркменабад и др.). При выборе Туркменабада запрос к
  `/api/masters` автоматически включает мастеров из посёлков-спутников
  (это делает бэкенд — см. `01-backend.md` Шаг 3.2, менять клиентскую
  логику фильтрации не нужно).
- [ ] Шаг 7.2 (опционально, если продукт захочет точнее): под головным
  городом — сворачиваемый список посёлков для ручного сужения поиска
  (`parent_city_id` в ответе `/api/cities` это позволяет). Не блокирует MVP —
  можно отложить, головного города с авто-включением посёлков достаточно
  для одного региона на старте.

---

## Этап 8. Отзывы (парный этап к `01-backend.md` Этап 8)

- [x] Шаг 8.1: `src/utils/rating.js` (`renderStars`) + звёзды и `(N)` число
  отзывов в карточке `MastersListScreen` (и в `SearchScreen` — тот же
  паттерн карточки, для консистентности). Показывается только когда
  `reviews_count > 0`, чтобы не засорять карточки без отзывов.
- [x] Шаг 8.2: На `MasterDetailScreen` — средний рейтинг в шапке, список
  одобренных отзывов (звёзды + комментарий) в футере, форма «оставить
  отзыв» (звёзды-пикер + комментарий) — рендерится только если
  `user.role === 'client' && user.phone_verified_at`.

**Побочная находка, без которой форма отзыва была бы бесполезна.**
`useAuth.js@restoreToken` при восстановлении сессии из `SecureStore`
ставил `user = { token }` — без `role`/`phone_verified_at` (был помечен
`// TODO: fetch user profile to validate token`). Форма отзыва не могла бы
понять, кому себя показывать, для любого клиента, который не залогинился
только что, а открыл уже установленное приложение — то есть почти всегда.
Добавлен `GET /api/me` на бэкенде (`01-backend.md`, Этап 8), `restoreToken()`
переписан на реальный запрос профиля.

Не покрыто автотестами (в `ServiceApp` по-прежнему нет JS-тестовой
инфраструктуры) — проверено синтаксис-парсингом всех изменённых файлов,
логику стоит проверить руками: отзыв от клиента с подтверждённым телефоном
показывает форму, от мастера/admin/неверифицированного клиента — нет,
отправленный отзыв не появляется в списке сразу (ждёт approve в админке).

---

## Сводная таблица зависимостей

| Что делаем | Зависит от |
|---|---|
| Этап 4 (поиск) | `01-backend.md` Этап 6 (`?q=`) |
| Этап 6 (Universal Links) | `01-backend.md` Этап 4A (домен, `.well-known`) |
| Этап 7 (посёлки) | `01-backend.md` Этап 1.2.1 (`parent_city_id`) |
| Этап 8 (отзывы) | Реализован; `01-backend.md` `GET /api/me` (новое) |
| Этап 3 (удаление фото) | `01-backend.md` Этап 7 |
