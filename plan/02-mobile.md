# Мобильное приложение (Expo / React Native) — единый план

Решение зафиксировано в `README.md`, п.2: остаёмся на Expo/React Native,
не переходим на Vue-стек. Причина коротко — в `ServiceApp/` уже рабочий
стек (Expo 56, RN 0.85, React Navigation 7) и ~10 экранов, переписывать
на Ionic+Vue значит выбросить сделанное ради смены синтаксиса, а не ради
нового результата.

Статус: `[x]` сделано и проверено по коду · `[~]` частично · `[ ]` не начато.

---

## Этап 1. Инициализация и навигация

- [x] Шаг 1.1: `npx create-expo-app ServiceApp` — сделано.
- [x] Шаг 1.2: `@react-navigation/native`, `bottom-tabs`, `native-stack` — установлены.
- [x] Шаг 1.3: Роутинг — `App.js` уже собран точно по плану:
  - `AuthStack` (Login, Register).
  - `MainTabs` (`Catalog` → `CatalogStack`, `Search`, `Profile` → `ProfileStack`).
- [x] Шаг 1.4: `expo-secure-store` + хук `useAuth` (`src/hooks/useAuth.js`) — проверяет токен при старте, переключает `AuthStack`/`MainTabs`.

---

## Этап 2. Экраны клиента (поиск мастеров)

- [x] Шаг 2.1: `CatalogScreen.js` — есть.
- [x] Шаг 2.2: `MastersListScreen.js` — есть.
- [x] Шаг 2.3: `MasterDetailScreen.js` — есть.

Проверить руками (best эффорт — реальным тестированием на устройстве/симуляторе, не только по наличию файла):
- [ ] Кнопка «Позвонить» реально дергает `Linking.openURL('tel:+993XXXXXXXX')`.
- [ ] `MastersListScreen` сортирует «свободен» выше «занят» (сервер уже так отдаёт — см. `01-backend.md` Шаг 3.2).

---

## Этап 3. Экраны мастера (управление визиткой)

- [x] Шаг 3.1: `MyProfileScreen.js`, `EditProfileScreen.js` — есть.
- [x] Шаг 3.2: `expo-image-picker` установлен — загрузка портфолио.
- [x] Шаг 3.3: `MyQRScreen.js` — есть.
  - [ ] Обновить экран после Этапа 4A в `01-backend.md` (QR теперь ведёт на `https://{домен}/m/{id}`, а не на `appscheme://` — сам экран не меняется, но убедиться, что он просто показывает картинку с бэкенда и не хардкодит схему).
- [ ] Кнопка удаления фото на `EditProfileScreen`/`MyProfileScreen` — после `01-backend.md` Этап 7 (`DELETE /api/profile/portfolio/{id}`).

**Про WebSocket-обновление статуса**: тумблер «Свободен/Занят» — обычный
REST (`POST /api/profile/status`), уже сделано. Laravel Reverb для
мгновенного обновления у всех клиентов — сознательно не в MVP, добавить
отдельным этапом при необходимости (не блокирует текущий план).

---

## Этап 4. Поиск (`SearchScreen`)

- [x] `SearchScreen.js` уже существует (таб `Search` подключён в `App.js`) — вариант «убрать таб» из исходного плана снят с повестки, экран есть.
- [ ] Убедиться, что `SearchScreen` реально дергает `GET /api/masters?q=&city_id=&category_id=` после того, как бэкенд получит `?q=` (`01-backend.md`, Этап 6). Сейчас экран может быть заглушкой без рабочего запроса — проверить.

---

## Этап 5. Offline-first (клиентский персист)

**Важно**: серверный `Cache::remember` в `01-backend.md` — это не офлайн,
это защита БД. Офлайн — это то, что храниться на устройстве.

- [ ] Шаг 5.1: Хранить `cities`/`categories` на устройстве
  (`AsyncStorage`, либо persist-плагин TanStack Query/Redux Persist —
  выбрать один, не тащить оба; для проекта такого размера хватит
  `@react-native-async-storage/async-storage` напрямую, без доп. библиотеки
  состояния — сейчас лишняя зависимость).
- [ ] Шаг 5.2: При старте `CatalogScreen` — сначала показать данные из
  локального кэша (мгновенно), в фоне обновить с сервера.
- [ ] Шаг 5.3: После версионирования справочников на бэкенде
  (`01-backend.md`, Шаг 3.1.1/версия или `ETag`) — тянуть полный объём
  только при изменении.

**Критерии приёмки**: открытие каталога без сети показывает ранее
загруженные города/категории.

---

## Этап 6. Universal Links / App Links (парный этап к `01-backend.md` Этап 4A)

- [ ] Шаг 6.1: `app.json`/`app.config.js`:
  - iOS: `associatedDomains: ["applinks:{домен}"]`.
  - Android: `intentFilters` с `autoVerify: true` на `https`-схему и хост домена.
- [ ] Шаг 6.2: Обработать входящий линк через `expo-linking`, смаппить `/m/{id}` на `MasterDetailScreen` (внутри `CatalogStack`, либо отдельный deep-link handler в `App.js`, который программно переключает навигацию).

**Критерии приёмки**: скан QR с установленным приложением открывает нужный экран мастера напрямую, без прохождения через веб-страницу.

---

## Этап 7. Выбор города с учётом посёлков-спутников (парный этап к `01-backend.md` Этап 1.2.1)

- [ ] Шаг 7.1: В `CatalogScreen` — выпадающий список городов показывает
  головные города (Туркменабад и др.). При выборе Туркменабада запрос к
  `/api/masters` автоматически включает мастеров из посёлков-спутников
  (это делает бэкенд — см. `01-backend.md` Шаг 3.2, менять клиентскую
  логику фильтрации не нужно).
- [ ] Шаг 7.2 (опционально, если продукт захочет точнее): под головным
  городом — сворачиваемый список посёлков для ручного сужения поиска
  (`parent_city_id` в ответе `/api/cities` это позволяет). Не блокирует MVP —
  можно отложить, головного города с авто-включением посёлков достаточно
  для одного региона на старте.

---

## Этап 8. Отзывы (парный этап к `01-backend.md` Этап 8)

- [ ] Шаг 8.1: Звёзды + число отзывов в карточке `MastersListScreen`.
- [ ] Шаг 8.2: Блок отзывов на `MasterDetailScreen` + форма «оставить отзыв» (доступна только авторизованному верифицированному `client`).

---

## Сводная таблица зависимостей

| Что делаем | Зависит от |
|---|---|
| Этап 4 (поиск) | `01-backend.md` Этап 6 (`?q=`) |
| Этап 6 (Universal Links) | `01-backend.md` Этап 4A (домен, `.well-known`) |
| Этап 7 (посёлки) | `01-backend.md` Этап 1.2.1 (`parent_city_id`) |
| Этап 8 (отзывы) | `01-backend.md` Этап 8, `01-backend.md` Этап 2A (verified client) |
| Этап 3 (удаление фото) | `01-backend.md` Этап 7 |
